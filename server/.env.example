# MongoDB Connection
MONGO_URI=mongodb://localhost:27017/focus-system
# For MongoDB Atlas (cloud), use:
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/focus-system

# JWT Secret (Change this to a random secure string in production)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development
